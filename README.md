Place the supplied `pcards.db` file in this folder before running the app locally.

This database is intentionally excluded from Git. It is approximately 99 MB and should be stored privately on the deployment host, not committed to a public repository.
